# 13 — Certify TUI and Teams MTK behavior with real E2E evidence

**What to build:** Produce the final evidence contract for Executable Skill Strategies by exercising the real local TUI, real Teams MTK owner control conversation and non-control origin, semantic continuation, deterministic and Hybrid execution, fail-closed repair escalation, and read-back of every external side effect before issuing a completion verdict.

**Blocked by:** 03 — Close non-control skill-mutation escape paths; 12 — Migrate bug-brief to an executable Hybrid Strategy.

**Status:** ready-for-agent

- [ ] All focused Agent, skill, registry, Gateway, authority, session/compression, Kanban, Background Review, and provider suites pass through the repository test wrapper with no hidden retry-only green result.
- [ ] The full applicable project suite and non-Python checks selected by the changed surfaces pass, or every unrelated/pre-existing failure is evidenced and reported separately.
- [ ] A real local TUI run explicitly loads and executes a harmless read-only contracted capability, and the response, provider read-back, model-call boundary, trace, and manifest digest agree.
- [ ] A real Teams MTK run proves the immutable owner sender in the exact control conversation can execute the same owner-level strategy under normal approvals.
- [ ] Real Teams MTK attempts from a non-control conversation and from a non-owner sender in the control conversation are denied before side effects and do not fall back adaptively.
- [ ] A resumed or compressed conversation with several older intents proves a long-delayed follow-up and correction select the intended work while an unrelated message starts a new Intent Thread.
- [ ] A safe live Contract Gap creates a redacted Kanban triage task; repeating the same fingerprint appends evidence to that task, and the task is read back to prove no auto-dispatch.
- [ ] A safe provider-failure scenario produces the distinct Provider Repair Task or, if the live backend cannot be safely forced, the highest real provider boundary plus an explicitly documented limitation supplies equivalent evidence.
- [ ] All temporary strategies, tasks, messages, and test artifacts are identified and safely cleaned up without deleting unrelated user state.
- [ ] The final verdict distinguishes pass, partial pass, flaky behavior, blocked credentials/network, authorization denial, and unrelated failure; no source inspection or mock-only claim substitutes for real execution evidence.
