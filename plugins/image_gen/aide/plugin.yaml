name: aide
version: 1.0.0
description: "MTK AIDE Gateway image generation (Azure OpenAI convention: gpt-image-2, Dalle3) via the internal gateway. Auth reuses providers.aide (CCH api_key_helper + X-User-Id). Saves images to $HERMES_HOME/cache/images/."
author: MTK
kind: backend
