name: aide
version: 1.0.0
description: "MTK AIDE Gateway text-to-speech (Azure OpenAI convention: aide-tts / aide-tts-hd) via the internal gateway. Auth reuses providers.aide (CCH api_key_helper + X-User-Id)."
author: MTK
kind: backend
